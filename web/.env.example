# Base URL of the Ofren Collect API the SPA talks to.
# Local dev: the API runs on http://localhost:5080 (the default below).
# Production (Vercel/Netlify): set this to your deployed API URL, e.g.
#   VITE_API_URL=https://ofren-collect-api.onrender.com
VITE_API_URL=http://localhost:5080
